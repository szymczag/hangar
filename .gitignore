node_modules
.next
.yarn

### NextJS ###
# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
dist/
out/
build/
.react-router/

# Misc
.DS_Store
*.pem
.history
tsconfig.tsbuildinfo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Turborepo
.turbo

## Django ##
venv
.venv
*.pyc
staticfiles
mediafiles
.env
.DS_Store
logs/
htmlcov/
.coverage

node_modules/
assets/dist/
npm-debug.log
yarn-error.log
pnpm-debug.log

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
package-lock.json
.vscode

# Sentry
.sentryclirc

# lock files
package-lock.json



.secrets
tmp/

## packages
dist
.temp/
deploy/selfhost/plane-app/

## Storybook
*storybook.log
output.css

dev-editor
# Redis
*.rdb
*.rdb.gz

storybook-static

CLAUDE.md

build/
.react-router/

build/
.react-router/
temp/
# Bare `scripts/` matches a directory of that name at *any* depth, not just the
# one at the root -- which silently kept apps/visual-tests/scripts out of the
# repository, so CI ran a lint script that was not there. Anchored to the root,
# with the handful of tracked files named so the pre-commit hook can stage the
# formatting it applies to them; it aborts on a path git refuses.
/scripts/
!scripts/branding-allowlist.json
!scripts/check-branding.mjs
!scripts/check-branding.test.mjs
!scripts/community-billing-page.test.mjs
!scripts/community-modal.test.mjs
!scripts/generate-release-notes.mjs
!scripts/vr.mjs
!packages/i18n/scripts/
!.github/scripts/
!.github/scripts/release_metadata.py
!.github/scripts/release_notes.py
!.github/scripts/tests/
!.github/scripts/tests/test_release_metadata.py
!.github/scripts/tests/test_release_notes.py
!.github/scripts/tests/test_release_workflow.py
!charts/hangar/scripts/

# i18n auto-generated types (regenerated on every build)
packages/i18n/src/types/keys.generated.ts

# Local security notes (not for version control)
/security/
/sast/

# Workstation-local state and linked worktrees
/.local/
