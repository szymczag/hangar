{
  "name": "@hangar/visual-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "vr": "playwright test",
    "vr:update": "playwright test --update-snapshots",
    "check:lint": "node scripts/check-suite-invariants.mjs",
    "check:types": "tsc --noEmit",
    "check:format": "oxfmt --check ."
  },
  "dependencies": {
    "admin": "workspace:*",
    "web": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "typescript": "catalog:"
  }
}
